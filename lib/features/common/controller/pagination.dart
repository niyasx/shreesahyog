// import '../../preBidding/preBidding2/apiModels/pre_bid_2_model.dart';
// import '../table/table_widgets.dart';
//
// int currentPage = 1;
// int pageSize = int.parse(selectedDropdownValue);
//
// List<PreBid2ModelResponseList> getCurrentPageItems(List<PreBid2ModelResponseList> items) {
//   final startIndex = (currentPage - 1) * pageSize;
//   final endIndex = startIndex + pageSize > items.length ? items.length : startIndex + pageSize;
//   return items.sublist(startIndex, endIndex);
// }
